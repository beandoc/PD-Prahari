
import type { PatientData } from '@/lib/types';
// This file is now primarily for exporting types if needed elsewhere,
// or for holding a template for patient data structure.
// The primary source of truth is now patient-data.json

// Keeping the type export is useful.
export type { PatientData };
